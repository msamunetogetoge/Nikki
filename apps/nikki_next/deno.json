{
  "tasks": {
    "dev": "deno run -A --node-modules-dir=auto -C . npm:next dev --hostname 0.0.0.0 --port 3000",
    "build": "deno run -A --node-modules-dir=auto -C . npm:next build",
    "start": "deno run -A --node-modules-dir=auto -C . npm:next start",
    "lint": "deno run -A --node-modules-dir=auto -C . npm:next lint"
  }
}
